{
  "name": "gif-fox",
  "version": "1.0.0",
  "description": "A Firefox browser extension to search and select GIF images to embed into chats, social media, etc.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "deploy": "npm run build && zip -r gif-fox.zip manifest.json icons/ dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.3",
    "vite": "^8.0.16"
  }
}
